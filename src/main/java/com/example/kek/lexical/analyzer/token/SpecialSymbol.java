package com.example.kek.lexical.analyzer.token;

public class SpecialSymbol extends Token {
    public SpecialSymbol(String code, int curLine, int curPos ) {
        super(code, curLine, curPos);
    }
}
