routine mainst(var a : integer, var b : integer) : boolean is
    a := 1
    b := a + 1
    if a > 0
        then return a + recursiveFunction(a - 1)
        else return a
    end
end

routine main(var a : integer, var b : integer) : boolean is
    a := 1
    b := a + 1
    if a > 0
        then return a + recursiveFunction(a - 1)
        else return a
    end
end